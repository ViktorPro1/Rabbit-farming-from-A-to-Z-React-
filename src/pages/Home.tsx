import Hero from "../components/Hero/Hero";
import SectionCards from "../components/SectionCards/SectionCards";

const Home = () => {
  return (
    <main>
      <Hero />
      <SectionCards />
    </main>
  );
};

export default Home;
